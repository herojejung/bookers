<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <!-- CSSファイルの読み込み -->
    <link rel="stylesheet" href="application.css" />
    <title>books</title>
  </head>
  <body>
   <h1 class= "header">
     Books
   <h1>
    <table>
    <thead>
    <tr>
    <th>Title</th>
    <th>Body</th>
    <th></th>
    </tr>
    </thead>
    <tbody>
    <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
    <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
    <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
    <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
    <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
       <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
    <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
    <tr>
    <% @books.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <% end %>
    <td>show</td>
    <td>Edit</td>
    <td>Destroy</td>
    </tr>
    </tbody>
    </table>

   <div class="form">
    <h2>New Book</h2>
   </div>
   <div class="form1">
      <%= form_with model: Book.new do |f| %>
    <h4>title</h4>
      <%= f.text_field :title %>
    <h4>body</h4>
      <%= f.text_area :body %>
      <%= f.submit 'Create Book' %>
    <% end %>
   </div>
  </body>
</html>
